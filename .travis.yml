language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t rportillo5/docker-react -f Dockerfile.dev  .

script:
  - docker run -e CI=true rportillo5/docker-react npm run test
